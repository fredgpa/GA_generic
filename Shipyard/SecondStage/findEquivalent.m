function [department] = findEquivalent(costArray, departments, materials)

    if length(costArray) > 1
        for i=1:length(costArray)
            
        end
    else
        department = departments(costArray);
    end

end