function [bool] = checkConstraints(ind,dim)

    load('Shipyard\constraint_table.mat');
    
    for i= 1:length(constraints)
        pos1 = nonzeros([ind(:)==constraints(i,1)]);
        pos2 = nonzeros([ind(:)==constraints(i,2)]);
        
        if constraints(i,4)
            if pos2 == pos1 + 1 || pos2 == pos1 - 1 || pos2 == pos1 + dim(2) || pos2 == pos1 - dim(2)
        else
            
        end
    end

end